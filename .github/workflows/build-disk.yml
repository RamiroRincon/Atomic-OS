---
name: Build disk images

on:
  workflow_dispatch:
    inputs:
      platform:
        required: true
        type: choice
        options:
          - amd64
          - arm64
  pull_request:
    branches:
      - main

env:
  IMAGE_NAME: ${{ github.event.repository.name }}
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "latest"
  BIB_IMAGE: "quay.io/centos-bootc/bootc-image-builder:latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    name: Build disk images
    runs-on: ${{ inputs.platform == 'amd64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      fail-fast: false
      matrix:
        disk-type: ["qcow2", "anaconda-iso"]

    permissions:
      contents: write
      packages: read
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          echo "USER_UID=$(id -u)" >> ${GITHUB_ENV}
          echo "USER_GID=$(id -g)" >> ${GITHUB_ENV}
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      - name: Maximize build space
        if: inputs.platform != 'arm64'
        uses: ublue-os/remove-unwanted-software@v9
        with:
          remove-codeql: true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Build disk images
        id: build
        uses: osbuild/bootc-image-builder-action@main
        with:
          builder-image: ${{ env.BIB_IMAGE }}
          config-file: ./disk_config/iso-gnome.toml
          image: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}
          chown: ${{ env.USER_UID }}:${{ env.USER_GID }}
          types: ${{ matrix.disk-type }}
          additional-args: --use-librepo=True --rootfs xfs

      - name: Prepare Artifacts
        run: |
          mkdir -p ./dist
          OUTPUT_DIR="${{ steps.build.outputs.output-directory }}"
          if [ "${{ matrix.disk-type }}" == "anaconda-iso" ]; then
            ISO_PATH=$(find "$OUTPUT_DIR" -name "install.iso" | head -n 1)
            cp "$ISO_PATH" "./dist/Atomic-OS.iso"
          else
            QCOW_PATH=$(find "$OUTPUT_DIR" -name "*.qcow2" | head -n 1)
            cp "$QCOW_PATH" "./dist/Atomic-OS.qcow2"
          fi

      - name: Upload disk images to Job Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Atomic-OS-${{ matrix.disk-type }}
          path: ./dist/
          retention-days: 1

  publish:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          pattern: Atomic-OS-*
          merge-multiple: true

      - name: Generate Release Tag
        id: tag
        run: echo "release_tag=$(date +'%Y.%m.%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Create Release and Upload
        env:
          # IMPORTANT: We are now using your Personal Access Token to bypass the 2GB limit
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
          TAG: ${{ steps.tag.outputs.release_tag }}
        run: |
          gh release create "$TAG" \
            --title "Atomic-OS Build $TAG" \
            --notes "Automated build of Atomic-OS. Direct downloads for ISO and QCOW2." \
            --prerelease

          echo "Uploading ISO (One single file)..."
          gh release upload "$TAG" ./artifacts/Atomic-OS.iso --clobber
          
          echo "Uploading QCOW2 (One single file)..."
          gh release upload "$TAG" ./artifacts/Atomic-OS.qcow2 --clobber